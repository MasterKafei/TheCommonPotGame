{% extends '@Layout/Page/Default/base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-12">
            {{ form_start(form) }}
            <div class="card">
                <div class="card-header">
                    Inscription
                </div>
                <div class="card-body">
                    <div class="form-group row">
                        <label class="col-sm-6 col-lg-4 col-form-label">{{ form_label(form.username) }}</label>
                        <div class="col-sm-6 col-lg-8">
                            {{ form_widget(form.username) }}
                        </div>
                    </div>
                    <div class="form-group row">
                        <label class="col-sm-6 col-lg-4 col-form-label">{{ form_label(form.plainPassword.first) }}</label>
                        <div class="col-sm-6 col-lg-8">
                            {{ form_widget(form.plainPassword.first) }}
                        </div>
                    </div>
                    <div class="form-group row">
                        <label class="col-sm-6 col-lg-4 col-form-label">{{ form_label(form.plainPassword.second) }}</label>
                        <div class="col-sm-6 col-lg-8">
                            {{ form_widget(form.plainPassword.second) }}
                        </div>
                    </div>
                </div>
                <input class="card-footer btn btn-primary bg-primary text-white" type="submit" value="S'inscrire">
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock body %}